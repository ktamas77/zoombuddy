[project]
name = "zoombuddy-sidecar"
version = "0.1.0"
description = "ZoomBuddy lip-sync sidecar (Wav2Lip on MPS/CPU)"
requires-python = ">=3.11,<3.13"
dependencies = [
  "torch>=2.4",
  "numpy",
  "opencv-python-headless",
  "librosa>=0.10",
  "soundfile",
  "tqdm",
]
